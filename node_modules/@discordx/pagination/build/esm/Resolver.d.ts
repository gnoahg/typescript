import type { Pagination } from "./Pagination.js";
import type { PaginationItem } from "./types.js";
export declare type Resolver = (page: number, pagination: Pagination) => PaginationItem | Promise<PaginationItem>;
export declare class PaginationResolver<T extends Resolver = Resolver> {
    resolver: T;
    maxLength: number;
    constructor(resolver: T, maxLength: number);
}
