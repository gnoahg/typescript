import { DIService } from "@discordx/di";
import KoaClient from "koa";
import { MetadataStorage } from "./logic/metadata.js";
export declare class Koa extends KoaClient {
    private _api;
    get api(): string;
    set api(value: string);
    get di(): DIService;
    get instance(): MetadataStorage;
    build(): Promise<void>;
}
