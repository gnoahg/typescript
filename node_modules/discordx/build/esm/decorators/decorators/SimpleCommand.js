import { DSimpleCommand, MetadataStorage } from "../../index.js";
export function SimpleCommand(name, params) {
    return function (target, key) {
        name = name ?? key;
        const cmd = DSimpleCommand.create(name, params?.aliases, params?.argSplitter, params?.botIds, params?.defaultPermission, params?.description, params?.directMessage, params?.guilds, params?.prefix).decorate(target.constructor, key, target[key]);
        MetadataStorage.instance.addSimpleCommand(cmd);
    };
}
//# sourceMappingURL=SimpleCommand.js.map