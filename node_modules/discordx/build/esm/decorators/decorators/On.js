import { DOn, MetadataStorage } from "../../index.js";
export function On(event, params) {
    return function (target, key, descriptor) {
        const myClass = target;
        const on = DOn.create(event, false, params?.botIds).decorate(myClass.constructor, key, descriptor?.value);
        MetadataStorage.instance.addOn(on);
    };
}
//# sourceMappingURL=On.js.map