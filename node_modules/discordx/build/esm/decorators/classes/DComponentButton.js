import { Method } from "./Method.js";
/**
 * @category Decorator
 */
export class DComponentButton extends Method {
    _id;
    _guilds;
    _botIds;
    get botIds() {
        return this._botIds;
    }
    set botIds(value) {
        this._botIds = value;
    }
    get id() {
        return this._id;
    }
    set id(value) {
        this._id = value;
    }
    get guilds() {
        return this._guilds;
    }
    set guilds(value) {
        this._guilds = value;
    }
    constructor(id, guilds, botIds) {
        super();
        this._id = id;
        this._guilds = guilds ?? [];
        this._botIds = botIds ?? [];
    }
    static create(id, guilds, botIds) {
        return new DComponentButton(id, guilds, botIds);
    }
    isId(text) {
        return typeof this.id === "string" ? this.id === text : this.id.test(text);
    }
    parseParams() {
        return [];
    }
}
//# sourceMappingURL=DComponentButton.js.map