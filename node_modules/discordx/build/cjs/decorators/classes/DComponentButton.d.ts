import type { IGuild } from "../../index.js";
import { Method } from "./Method.js";
/**
 * @category Decorator
 */
export declare class DComponentButton extends Method {
    private _id;
    private _guilds;
    private _botIds;
    get botIds(): string[];
    set botIds(value: string[]);
    get id(): string | RegExp;
    set id(value: string | RegExp);
    get guilds(): IGuild[];
    set guilds(value: IGuild[]);
    protected constructor(id: string | RegExp, guilds?: IGuild[], botIds?: string[]);
    static create(id: string | RegExp, guilds?: IGuild[], botIds?: string[]): DComponentButton;
    isId(text: string): boolean;
    parseParams(): never[];
}
