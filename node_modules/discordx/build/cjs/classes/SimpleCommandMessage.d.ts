import type { Message } from "discord.js";
import type { ArgSplitter, DSimpleCommand, SimpleCommandOptionType } from "../index.js";
/**
 * Simple command message class
 */
export declare class SimpleCommandMessage {
    prefix: string | RegExp;
    argString: string;
    message: Message;
    info: DSimpleCommand;
    splitter?: ArgSplitter | undefined;
    options: SimpleCommandOptionType[];
    constructor(prefix: string | RegExp, argString: string, message: Message, info: DSimpleCommand, splitter?: ArgSplitter | undefined);
    get name(): string;
    get description(): string;
    /**
     * Verify that all options are valid
     * @returns
     */
    isValid(): boolean;
    /**
     * get related commands
     * @returns
     */
    getRelatedCommands(): DSimpleCommand[];
    /**
     * send usage syntax for command
     * @returns
     */
    sendUsageSyntax(): Promise<Message>;
}
